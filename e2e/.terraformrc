provider_installation {
  dev_overrides {
    # Points at the repository root where `go build` drops the provider binary.
    "TOKIUM/ddff" = "/home/higashi_y/projects/terraform-provider-ddff"
  }

  direct {}
}
