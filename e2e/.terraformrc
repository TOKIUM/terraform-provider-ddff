provider_installation {
  dev_overrides {
    # Points at the repository root where `go build` drops the provider binary.
    "TOKIUM/datadog-feature-flags" = "/home/higashi_y/projects/terraform-provider-datadog-feature-flags"
  }

  direct {}
}
